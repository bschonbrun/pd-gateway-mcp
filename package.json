{
  "name": "pd-gateway-mcp",
  "version": "1.0.0",
  "description": "Lightweight MCP gateway for Pipedream — full automation lifecycle through 16 tools instead of 10,000+",
  "main": "dist/index.js",
  "bin": {
    "pd-gateway-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "test": "tsc && node --test dist/__tests__/*.js"
  },
  "keywords": ["mcp", "pipedream", "gateway", "ai", "automation"],
  "author": "your-username",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}
